import { View, Text } from "react-native";
import React from "react";
import { Center } from "native-base";

const Scan = () => {
  return (
    <Center>
      <Text>Scan</Text>
    </Center>
  );
};

export default Scan;
